# Task: Apply New Purple Logo Palette

## Files Modified

### `templates/base.html`

- Replaced all instances of the old primary green color (`#184C36`) with the new saffron purple (`#8A609C`).
- Replaced all instances of the old dark green hover color (`#0b251a`) with the new darker purple (`#6D4C7D`).
- Updated the `.header-promo-bar` gradient to use the new purple colors.

### `templates/includes/header.html`

- Updated the `background` of the `.header-promo-bar` to use the new purple gradient.
- Updated the `border-color` of the language switcher button and the `background-color` of the hamburger menu button to use the new primary purple color.

## Terminal Commands

No terminal commands are required for this task.
